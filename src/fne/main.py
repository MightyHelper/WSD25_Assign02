# NOTE: This file was created as an adapter during scaffolding.
# The canonical application package is `app` and the ASGI app is provided by `app.main:app`.
# Keep this module as a lightweight adapter for compatibility only.
from app.main import app  # re-export the application under fne.main for backwards compatibility

